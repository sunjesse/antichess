import chess
import utils

def eval(board, n):
	"""
	Input:
		board - chess.Board object
		n - move count. We will deprecate this param once we implement evaluation metric.
	Returns:
		A valid move in san string format.
	"""
	return utils.get_input(n)	
